describe(__filename, () => {

  before(() => {
    throw new Error('error');
  });

  after(() => {
    throw new Error('error');
  });

  beforeEach(() => {
    throw new Error('error');
  });

  afterEach(() => {
    throw new Error('error');
  });

});
