describe(__filename, function() {

  before(() => 'no name in before');

  after(() => 'no name in after');

  beforeEach(() => 'no name in beforeEach');

  afterEach(() => 'no name in afterEach');

});
